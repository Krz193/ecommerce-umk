import 'package:flutter/material.dart';

class AppColors {
  // Soft, warm orange palette
  static const primary = Color(0xFFF97316); // Soft Warm Orange
  static const primaryHover = Color(0xFFEA580C);
  static const primaryLight = Color(0xFFFFF3EB); // Soft Orange Tint
  static const primarySurface = Color(0xFFFFF8F5);

  static const accent = Color(0xFFFB923C);

  // Background & Cards
  static const background = Color(0xFFF8F9FA); // Very light crisp background
  static const surface = Colors.white;

  // Text
  static const textPrimary = Color(
    0xFF111827,
  ); // Deep Charcoal/Black for legibility
  static const textSecondary = Color(0xFF6B7280); // Muted Slate Gray
  static const textMuted = Color(0xFF9CA3AF);

  // Status
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);
}
